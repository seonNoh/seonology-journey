// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               unknown
// source: journey/v1/external.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Timestamp } from "../../google/protobuf/timestamp";
import { GeoPoint, Money } from "./common";

export const protobufPackage = "journey.v1";

export interface TripStatistics {
  tripId: string;
  totalDays: number;
  totalSchedules: number;
  totalMeals: number;
  totalExpense?: Money | undefined;
  totalDistanceKm: number;
  totalPhotos: number;
  visitedRegions: number;
}

export interface GetTripStatisticsRequest {
  tripId: string;
}

export interface GetTripStatisticsResponse {
  stats?: TripStatistics | undefined;
}

export interface YearlyStatistics {
  year: number;
  trips: number;
  days: number;
  totalExpense?: Money | undefined;
  countries: string[];
}

export interface GetYearlyStatisticsRequest {
  year: number;
}

export interface GetYearlyStatisticsResponse {
  stats?: YearlyStatistics | undefined;
}

export interface GeocodeRequest {
  query: string;
}

export interface GeocodePlace {
  placeId: string;
  name: string;
  address: string;
  location?: GeoPoint | undefined;
}

export interface GeocodeResponse {
  places: GeocodePlace[];
}

export interface ReverseGeocodeRequest {
  location?: GeoPoint | undefined;
}

export interface ReverseGeocodeResponse {
  place?: GeocodePlace | undefined;
}

export interface GetExchangeRateRequest {
  /** KRW */
  base: string;
  /** JPY */
  target: string;
  /** YYYY-MM-DD (optional) */
  date: string;
}

export interface GetExchangeRateResponse {
  base: string;
  target: string;
  rate: number;
  fetchedAt?: Date | undefined;
}

export interface GetWeatherForecastRequest {
  location?:
    | GeoPoint
    | undefined;
  /** YYYY-MM-DD */
  date: string;
}

export interface GetWeatherForecastResponse {
  condition: string;
  temperatureHigh: number;
  temperatureLow: number;
  precipitationProb: number;
  raw: string;
}

function createBaseTripStatistics(): TripStatistics {
  return {
    tripId: "",
    totalDays: 0,
    totalSchedules: 0,
    totalMeals: 0,
    totalExpense: undefined,
    totalDistanceKm: 0,
    totalPhotos: 0,
    visitedRegions: 0,
  };
}

export const TripStatistics: MessageFns<TripStatistics> = {
  encode(message: TripStatistics, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.tripId !== "") {
      writer.uint32(10).string(message.tripId);
    }
    if (message.totalDays !== 0) {
      writer.uint32(16).int32(message.totalDays);
    }
    if (message.totalSchedules !== 0) {
      writer.uint32(24).int32(message.totalSchedules);
    }
    if (message.totalMeals !== 0) {
      writer.uint32(32).int32(message.totalMeals);
    }
    if (message.totalExpense !== undefined) {
      Money.encode(message.totalExpense, writer.uint32(42).fork()).join();
    }
    if (message.totalDistanceKm !== 0) {
      writer.uint32(49).double(message.totalDistanceKm);
    }
    if (message.totalPhotos !== 0) {
      writer.uint32(56).int32(message.totalPhotos);
    }
    if (message.visitedRegions !== 0) {
      writer.uint32(64).int32(message.visitedRegions);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TripStatistics {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTripStatistics();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.tripId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.totalDays = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.totalSchedules = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.totalMeals = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.totalExpense = Money.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 49) {
            break;
          }

          message.totalDistanceKm = reader.double();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.totalPhotos = reader.int32();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.visitedRegions = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TripStatistics {
    return {
      tripId: isSet(object.tripId)
        ? globalThis.String(object.tripId)
        : isSet(object.trip_id)
        ? globalThis.String(object.trip_id)
        : "",
      totalDays: isSet(object.totalDays)
        ? globalThis.Number(object.totalDays)
        : isSet(object.total_days)
        ? globalThis.Number(object.total_days)
        : 0,
      totalSchedules: isSet(object.totalSchedules)
        ? globalThis.Number(object.totalSchedules)
        : isSet(object.total_schedules)
        ? globalThis.Number(object.total_schedules)
        : 0,
      totalMeals: isSet(object.totalMeals)
        ? globalThis.Number(object.totalMeals)
        : isSet(object.total_meals)
        ? globalThis.Number(object.total_meals)
        : 0,
      totalExpense: isSet(object.totalExpense)
        ? Money.fromJSON(object.totalExpense)
        : isSet(object.total_expense)
        ? Money.fromJSON(object.total_expense)
        : undefined,
      totalDistanceKm: isSet(object.totalDistanceKm)
        ? globalThis.Number(object.totalDistanceKm)
        : isSet(object.total_distance_km)
        ? globalThis.Number(object.total_distance_km)
        : 0,
      totalPhotos: isSet(object.totalPhotos)
        ? globalThis.Number(object.totalPhotos)
        : isSet(object.total_photos)
        ? globalThis.Number(object.total_photos)
        : 0,
      visitedRegions: isSet(object.visitedRegions)
        ? globalThis.Number(object.visitedRegions)
        : isSet(object.visited_regions)
        ? globalThis.Number(object.visited_regions)
        : 0,
    };
  },

  toJSON(message: TripStatistics): unknown {
    const obj: any = {};
    if (message.tripId !== "") {
      obj.tripId = message.tripId;
    }
    if (message.totalDays !== 0) {
      obj.totalDays = Math.round(message.totalDays);
    }
    if (message.totalSchedules !== 0) {
      obj.totalSchedules = Math.round(message.totalSchedules);
    }
    if (message.totalMeals !== 0) {
      obj.totalMeals = Math.round(message.totalMeals);
    }
    if (message.totalExpense !== undefined) {
      obj.totalExpense = Money.toJSON(message.totalExpense);
    }
    if (message.totalDistanceKm !== 0) {
      obj.totalDistanceKm = message.totalDistanceKm;
    }
    if (message.totalPhotos !== 0) {
      obj.totalPhotos = Math.round(message.totalPhotos);
    }
    if (message.visitedRegions !== 0) {
      obj.visitedRegions = Math.round(message.visitedRegions);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TripStatistics>, I>>(base?: I): TripStatistics {
    return TripStatistics.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TripStatistics>, I>>(object: I): TripStatistics {
    const message = createBaseTripStatistics();
    message.tripId = object.tripId ?? "";
    message.totalDays = object.totalDays ?? 0;
    message.totalSchedules = object.totalSchedules ?? 0;
    message.totalMeals = object.totalMeals ?? 0;
    message.totalExpense = (object.totalExpense !== undefined && object.totalExpense !== null)
      ? Money.fromPartial(object.totalExpense)
      : undefined;
    message.totalDistanceKm = object.totalDistanceKm ?? 0;
    message.totalPhotos = object.totalPhotos ?? 0;
    message.visitedRegions = object.visitedRegions ?? 0;
    return message;
  },
};

function createBaseGetTripStatisticsRequest(): GetTripStatisticsRequest {
  return { tripId: "" };
}

export const GetTripStatisticsRequest: MessageFns<GetTripStatisticsRequest> = {
  encode(message: GetTripStatisticsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.tripId !== "") {
      writer.uint32(10).string(message.tripId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetTripStatisticsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetTripStatisticsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.tripId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetTripStatisticsRequest {
    return {
      tripId: isSet(object.tripId)
        ? globalThis.String(object.tripId)
        : isSet(object.trip_id)
        ? globalThis.String(object.trip_id)
        : "",
    };
  },

  toJSON(message: GetTripStatisticsRequest): unknown {
    const obj: any = {};
    if (message.tripId !== "") {
      obj.tripId = message.tripId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetTripStatisticsRequest>, I>>(base?: I): GetTripStatisticsRequest {
    return GetTripStatisticsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetTripStatisticsRequest>, I>>(object: I): GetTripStatisticsRequest {
    const message = createBaseGetTripStatisticsRequest();
    message.tripId = object.tripId ?? "";
    return message;
  },
};

function createBaseGetTripStatisticsResponse(): GetTripStatisticsResponse {
  return { stats: undefined };
}

export const GetTripStatisticsResponse: MessageFns<GetTripStatisticsResponse> = {
  encode(message: GetTripStatisticsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.stats !== undefined) {
      TripStatistics.encode(message.stats, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetTripStatisticsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetTripStatisticsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.stats = TripStatistics.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetTripStatisticsResponse {
    return { stats: isSet(object.stats) ? TripStatistics.fromJSON(object.stats) : undefined };
  },

  toJSON(message: GetTripStatisticsResponse): unknown {
    const obj: any = {};
    if (message.stats !== undefined) {
      obj.stats = TripStatistics.toJSON(message.stats);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetTripStatisticsResponse>, I>>(base?: I): GetTripStatisticsResponse {
    return GetTripStatisticsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetTripStatisticsResponse>, I>>(object: I): GetTripStatisticsResponse {
    const message = createBaseGetTripStatisticsResponse();
    message.stats = (object.stats !== undefined && object.stats !== null)
      ? TripStatistics.fromPartial(object.stats)
      : undefined;
    return message;
  },
};

function createBaseYearlyStatistics(): YearlyStatistics {
  return { year: 0, trips: 0, days: 0, totalExpense: undefined, countries: [] };
}

export const YearlyStatistics: MessageFns<YearlyStatistics> = {
  encode(message: YearlyStatistics, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.year !== 0) {
      writer.uint32(8).int32(message.year);
    }
    if (message.trips !== 0) {
      writer.uint32(16).int32(message.trips);
    }
    if (message.days !== 0) {
      writer.uint32(24).int32(message.days);
    }
    if (message.totalExpense !== undefined) {
      Money.encode(message.totalExpense, writer.uint32(34).fork()).join();
    }
    for (const v of message.countries) {
      writer.uint32(42).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): YearlyStatistics {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseYearlyStatistics();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.year = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.trips = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.days = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.totalExpense = Money.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.countries.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): YearlyStatistics {
    return {
      year: isSet(object.year) ? globalThis.Number(object.year) : 0,
      trips: isSet(object.trips) ? globalThis.Number(object.trips) : 0,
      days: isSet(object.days) ? globalThis.Number(object.days) : 0,
      totalExpense: isSet(object.totalExpense)
        ? Money.fromJSON(object.totalExpense)
        : isSet(object.total_expense)
        ? Money.fromJSON(object.total_expense)
        : undefined,
      countries: globalThis.Array.isArray(object?.countries)
        ? object.countries.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: YearlyStatistics): unknown {
    const obj: any = {};
    if (message.year !== 0) {
      obj.year = Math.round(message.year);
    }
    if (message.trips !== 0) {
      obj.trips = Math.round(message.trips);
    }
    if (message.days !== 0) {
      obj.days = Math.round(message.days);
    }
    if (message.totalExpense !== undefined) {
      obj.totalExpense = Money.toJSON(message.totalExpense);
    }
    if (message.countries?.length) {
      obj.countries = message.countries;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<YearlyStatistics>, I>>(base?: I): YearlyStatistics {
    return YearlyStatistics.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<YearlyStatistics>, I>>(object: I): YearlyStatistics {
    const message = createBaseYearlyStatistics();
    message.year = object.year ?? 0;
    message.trips = object.trips ?? 0;
    message.days = object.days ?? 0;
    message.totalExpense = (object.totalExpense !== undefined && object.totalExpense !== null)
      ? Money.fromPartial(object.totalExpense)
      : undefined;
    message.countries = object.countries?.map((e) => e) || [];
    return message;
  },
};

function createBaseGetYearlyStatisticsRequest(): GetYearlyStatisticsRequest {
  return { year: 0 };
}

export const GetYearlyStatisticsRequest: MessageFns<GetYearlyStatisticsRequest> = {
  encode(message: GetYearlyStatisticsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.year !== 0) {
      writer.uint32(8).int32(message.year);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetYearlyStatisticsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetYearlyStatisticsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.year = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetYearlyStatisticsRequest {
    return { year: isSet(object.year) ? globalThis.Number(object.year) : 0 };
  },

  toJSON(message: GetYearlyStatisticsRequest): unknown {
    const obj: any = {};
    if (message.year !== 0) {
      obj.year = Math.round(message.year);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetYearlyStatisticsRequest>, I>>(base?: I): GetYearlyStatisticsRequest {
    return GetYearlyStatisticsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetYearlyStatisticsRequest>, I>>(object: I): GetYearlyStatisticsRequest {
    const message = createBaseGetYearlyStatisticsRequest();
    message.year = object.year ?? 0;
    return message;
  },
};

function createBaseGetYearlyStatisticsResponse(): GetYearlyStatisticsResponse {
  return { stats: undefined };
}

export const GetYearlyStatisticsResponse: MessageFns<GetYearlyStatisticsResponse> = {
  encode(message: GetYearlyStatisticsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.stats !== undefined) {
      YearlyStatistics.encode(message.stats, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetYearlyStatisticsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetYearlyStatisticsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.stats = YearlyStatistics.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetYearlyStatisticsResponse {
    return { stats: isSet(object.stats) ? YearlyStatistics.fromJSON(object.stats) : undefined };
  },

  toJSON(message: GetYearlyStatisticsResponse): unknown {
    const obj: any = {};
    if (message.stats !== undefined) {
      obj.stats = YearlyStatistics.toJSON(message.stats);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetYearlyStatisticsResponse>, I>>(base?: I): GetYearlyStatisticsResponse {
    return GetYearlyStatisticsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetYearlyStatisticsResponse>, I>>(object: I): GetYearlyStatisticsResponse {
    const message = createBaseGetYearlyStatisticsResponse();
    message.stats = (object.stats !== undefined && object.stats !== null)
      ? YearlyStatistics.fromPartial(object.stats)
      : undefined;
    return message;
  },
};

function createBaseGeocodeRequest(): GeocodeRequest {
  return { query: "" };
}

export const GeocodeRequest: MessageFns<GeocodeRequest> = {
  encode(message: GeocodeRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.query !== "") {
      writer.uint32(10).string(message.query);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GeocodeRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGeocodeRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.query = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GeocodeRequest {
    return { query: isSet(object.query) ? globalThis.String(object.query) : "" };
  },

  toJSON(message: GeocodeRequest): unknown {
    const obj: any = {};
    if (message.query !== "") {
      obj.query = message.query;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GeocodeRequest>, I>>(base?: I): GeocodeRequest {
    return GeocodeRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GeocodeRequest>, I>>(object: I): GeocodeRequest {
    const message = createBaseGeocodeRequest();
    message.query = object.query ?? "";
    return message;
  },
};

function createBaseGeocodePlace(): GeocodePlace {
  return { placeId: "", name: "", address: "", location: undefined };
}

export const GeocodePlace: MessageFns<GeocodePlace> = {
  encode(message: GeocodePlace, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.placeId !== "") {
      writer.uint32(10).string(message.placeId);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.address !== "") {
      writer.uint32(26).string(message.address);
    }
    if (message.location !== undefined) {
      GeoPoint.encode(message.location, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GeocodePlace {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGeocodePlace();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.placeId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.address = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.location = GeoPoint.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GeocodePlace {
    return {
      placeId: isSet(object.placeId)
        ? globalThis.String(object.placeId)
        : isSet(object.place_id)
        ? globalThis.String(object.place_id)
        : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      address: isSet(object.address) ? globalThis.String(object.address) : "",
      location: isSet(object.location) ? GeoPoint.fromJSON(object.location) : undefined,
    };
  },

  toJSON(message: GeocodePlace): unknown {
    const obj: any = {};
    if (message.placeId !== "") {
      obj.placeId = message.placeId;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.address !== "") {
      obj.address = message.address;
    }
    if (message.location !== undefined) {
      obj.location = GeoPoint.toJSON(message.location);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GeocodePlace>, I>>(base?: I): GeocodePlace {
    return GeocodePlace.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GeocodePlace>, I>>(object: I): GeocodePlace {
    const message = createBaseGeocodePlace();
    message.placeId = object.placeId ?? "";
    message.name = object.name ?? "";
    message.address = object.address ?? "";
    message.location = (object.location !== undefined && object.location !== null)
      ? GeoPoint.fromPartial(object.location)
      : undefined;
    return message;
  },
};

function createBaseGeocodeResponse(): GeocodeResponse {
  return { places: [] };
}

export const GeocodeResponse: MessageFns<GeocodeResponse> = {
  encode(message: GeocodeResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.places) {
      GeocodePlace.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GeocodeResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGeocodeResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.places.push(GeocodePlace.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GeocodeResponse {
    return {
      places: globalThis.Array.isArray(object?.places) ? object.places.map((e: any) => GeocodePlace.fromJSON(e)) : [],
    };
  },

  toJSON(message: GeocodeResponse): unknown {
    const obj: any = {};
    if (message.places?.length) {
      obj.places = message.places.map((e) => GeocodePlace.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GeocodeResponse>, I>>(base?: I): GeocodeResponse {
    return GeocodeResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GeocodeResponse>, I>>(object: I): GeocodeResponse {
    const message = createBaseGeocodeResponse();
    message.places = object.places?.map((e) => GeocodePlace.fromPartial(e)) || [];
    return message;
  },
};

function createBaseReverseGeocodeRequest(): ReverseGeocodeRequest {
  return { location: undefined };
}

export const ReverseGeocodeRequest: MessageFns<ReverseGeocodeRequest> = {
  encode(message: ReverseGeocodeRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.location !== undefined) {
      GeoPoint.encode(message.location, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReverseGeocodeRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReverseGeocodeRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.location = GeoPoint.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ReverseGeocodeRequest {
    return { location: isSet(object.location) ? GeoPoint.fromJSON(object.location) : undefined };
  },

  toJSON(message: ReverseGeocodeRequest): unknown {
    const obj: any = {};
    if (message.location !== undefined) {
      obj.location = GeoPoint.toJSON(message.location);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ReverseGeocodeRequest>, I>>(base?: I): ReverseGeocodeRequest {
    return ReverseGeocodeRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ReverseGeocodeRequest>, I>>(object: I): ReverseGeocodeRequest {
    const message = createBaseReverseGeocodeRequest();
    message.location = (object.location !== undefined && object.location !== null)
      ? GeoPoint.fromPartial(object.location)
      : undefined;
    return message;
  },
};

function createBaseReverseGeocodeResponse(): ReverseGeocodeResponse {
  return { place: undefined };
}

export const ReverseGeocodeResponse: MessageFns<ReverseGeocodeResponse> = {
  encode(message: ReverseGeocodeResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.place !== undefined) {
      GeocodePlace.encode(message.place, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReverseGeocodeResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReverseGeocodeResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.place = GeocodePlace.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ReverseGeocodeResponse {
    return { place: isSet(object.place) ? GeocodePlace.fromJSON(object.place) : undefined };
  },

  toJSON(message: ReverseGeocodeResponse): unknown {
    const obj: any = {};
    if (message.place !== undefined) {
      obj.place = GeocodePlace.toJSON(message.place);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ReverseGeocodeResponse>, I>>(base?: I): ReverseGeocodeResponse {
    return ReverseGeocodeResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ReverseGeocodeResponse>, I>>(object: I): ReverseGeocodeResponse {
    const message = createBaseReverseGeocodeResponse();
    message.place = (object.place !== undefined && object.place !== null)
      ? GeocodePlace.fromPartial(object.place)
      : undefined;
    return message;
  },
};

function createBaseGetExchangeRateRequest(): GetExchangeRateRequest {
  return { base: "", target: "", date: "" };
}

export const GetExchangeRateRequest: MessageFns<GetExchangeRateRequest> = {
  encode(message: GetExchangeRateRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.base !== "") {
      writer.uint32(10).string(message.base);
    }
    if (message.target !== "") {
      writer.uint32(18).string(message.target);
    }
    if (message.date !== "") {
      writer.uint32(26).string(message.date);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetExchangeRateRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetExchangeRateRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.base = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.target = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.date = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetExchangeRateRequest {
    return {
      base: isSet(object.base) ? globalThis.String(object.base) : "",
      target: isSet(object.target) ? globalThis.String(object.target) : "",
      date: isSet(object.date) ? globalThis.String(object.date) : "",
    };
  },

  toJSON(message: GetExchangeRateRequest): unknown {
    const obj: any = {};
    if (message.base !== "") {
      obj.base = message.base;
    }
    if (message.target !== "") {
      obj.target = message.target;
    }
    if (message.date !== "") {
      obj.date = message.date;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetExchangeRateRequest>, I>>(base?: I): GetExchangeRateRequest {
    return GetExchangeRateRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetExchangeRateRequest>, I>>(object: I): GetExchangeRateRequest {
    const message = createBaseGetExchangeRateRequest();
    message.base = object.base ?? "";
    message.target = object.target ?? "";
    message.date = object.date ?? "";
    return message;
  },
};

function createBaseGetExchangeRateResponse(): GetExchangeRateResponse {
  return { base: "", target: "", rate: 0, fetchedAt: undefined };
}

export const GetExchangeRateResponse: MessageFns<GetExchangeRateResponse> = {
  encode(message: GetExchangeRateResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.base !== "") {
      writer.uint32(10).string(message.base);
    }
    if (message.target !== "") {
      writer.uint32(18).string(message.target);
    }
    if (message.rate !== 0) {
      writer.uint32(25).double(message.rate);
    }
    if (message.fetchedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.fetchedAt), writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetExchangeRateResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetExchangeRateResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.base = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.target = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 25) {
            break;
          }

          message.rate = reader.double();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.fetchedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetExchangeRateResponse {
    return {
      base: isSet(object.base) ? globalThis.String(object.base) : "",
      target: isSet(object.target) ? globalThis.String(object.target) : "",
      rate: isSet(object.rate) ? globalThis.Number(object.rate) : 0,
      fetchedAt: isSet(object.fetchedAt)
        ? fromJsonTimestamp(object.fetchedAt)
        : isSet(object.fetched_at)
        ? fromJsonTimestamp(object.fetched_at)
        : undefined,
    };
  },

  toJSON(message: GetExchangeRateResponse): unknown {
    const obj: any = {};
    if (message.base !== "") {
      obj.base = message.base;
    }
    if (message.target !== "") {
      obj.target = message.target;
    }
    if (message.rate !== 0) {
      obj.rate = message.rate;
    }
    if (message.fetchedAt !== undefined) {
      obj.fetchedAt = message.fetchedAt.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetExchangeRateResponse>, I>>(base?: I): GetExchangeRateResponse {
    return GetExchangeRateResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetExchangeRateResponse>, I>>(object: I): GetExchangeRateResponse {
    const message = createBaseGetExchangeRateResponse();
    message.base = object.base ?? "";
    message.target = object.target ?? "";
    message.rate = object.rate ?? 0;
    message.fetchedAt = object.fetchedAt ?? undefined;
    return message;
  },
};

function createBaseGetWeatherForecastRequest(): GetWeatherForecastRequest {
  return { location: undefined, date: "" };
}

export const GetWeatherForecastRequest: MessageFns<GetWeatherForecastRequest> = {
  encode(message: GetWeatherForecastRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.location !== undefined) {
      GeoPoint.encode(message.location, writer.uint32(10).fork()).join();
    }
    if (message.date !== "") {
      writer.uint32(18).string(message.date);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetWeatherForecastRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetWeatherForecastRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.location = GeoPoint.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.date = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetWeatherForecastRequest {
    return {
      location: isSet(object.location) ? GeoPoint.fromJSON(object.location) : undefined,
      date: isSet(object.date) ? globalThis.String(object.date) : "",
    };
  },

  toJSON(message: GetWeatherForecastRequest): unknown {
    const obj: any = {};
    if (message.location !== undefined) {
      obj.location = GeoPoint.toJSON(message.location);
    }
    if (message.date !== "") {
      obj.date = message.date;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetWeatherForecastRequest>, I>>(base?: I): GetWeatherForecastRequest {
    return GetWeatherForecastRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetWeatherForecastRequest>, I>>(object: I): GetWeatherForecastRequest {
    const message = createBaseGetWeatherForecastRequest();
    message.location = (object.location !== undefined && object.location !== null)
      ? GeoPoint.fromPartial(object.location)
      : undefined;
    message.date = object.date ?? "";
    return message;
  },
};

function createBaseGetWeatherForecastResponse(): GetWeatherForecastResponse {
  return { condition: "", temperatureHigh: 0, temperatureLow: 0, precipitationProb: 0, raw: "" };
}

export const GetWeatherForecastResponse: MessageFns<GetWeatherForecastResponse> = {
  encode(message: GetWeatherForecastResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.condition !== "") {
      writer.uint32(10).string(message.condition);
    }
    if (message.temperatureHigh !== 0) {
      writer.uint32(17).double(message.temperatureHigh);
    }
    if (message.temperatureLow !== 0) {
      writer.uint32(25).double(message.temperatureLow);
    }
    if (message.precipitationProb !== 0) {
      writer.uint32(33).double(message.precipitationProb);
    }
    if (message.raw !== "") {
      writer.uint32(42).string(message.raw);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetWeatherForecastResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetWeatherForecastResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.condition = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 17) {
            break;
          }

          message.temperatureHigh = reader.double();
          continue;
        }
        case 3: {
          if (tag !== 25) {
            break;
          }

          message.temperatureLow = reader.double();
          continue;
        }
        case 4: {
          if (tag !== 33) {
            break;
          }

          message.precipitationProb = reader.double();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.raw = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetWeatherForecastResponse {
    return {
      condition: isSet(object.condition) ? globalThis.String(object.condition) : "",
      temperatureHigh: isSet(object.temperatureHigh)
        ? globalThis.Number(object.temperatureHigh)
        : isSet(object.temperature_high)
        ? globalThis.Number(object.temperature_high)
        : 0,
      temperatureLow: isSet(object.temperatureLow)
        ? globalThis.Number(object.temperatureLow)
        : isSet(object.temperature_low)
        ? globalThis.Number(object.temperature_low)
        : 0,
      precipitationProb: isSet(object.precipitationProb)
        ? globalThis.Number(object.precipitationProb)
        : isSet(object.precipitation_prob)
        ? globalThis.Number(object.precipitation_prob)
        : 0,
      raw: isSet(object.raw) ? globalThis.String(object.raw) : "",
    };
  },

  toJSON(message: GetWeatherForecastResponse): unknown {
    const obj: any = {};
    if (message.condition !== "") {
      obj.condition = message.condition;
    }
    if (message.temperatureHigh !== 0) {
      obj.temperatureHigh = message.temperatureHigh;
    }
    if (message.temperatureLow !== 0) {
      obj.temperatureLow = message.temperatureLow;
    }
    if (message.precipitationProb !== 0) {
      obj.precipitationProb = message.precipitationProb;
    }
    if (message.raw !== "") {
      obj.raw = message.raw;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetWeatherForecastResponse>, I>>(base?: I): GetWeatherForecastResponse {
    return GetWeatherForecastResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetWeatherForecastResponse>, I>>(object: I): GetWeatherForecastResponse {
    const message = createBaseGetWeatherForecastResponse();
    message.condition = object.condition ?? "";
    message.temperatureHigh = object.temperatureHigh ?? 0;
    message.temperatureLow = object.temperatureLow ?? 0;
    message.precipitationProb = object.precipitationProb ?? 0;
    message.raw = object.raw ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}

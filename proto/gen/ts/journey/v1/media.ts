// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               unknown
// source: journey/v1/media.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Timestamp } from "../../google/protobuf/timestamp";
import { AuditTimestamps, GeoPoint, PageInfo, PageRequest } from "./common";

export const protobufPackage = "journey.v1";

/** Media - 사진/영상 메타 */
export interface Media {
  id: string;
  tripId: string;
  /** optional */
  dayId: string;
  /** optional */
  scheduleId: string;
  s3Key: string;
  thumbnailS3Key: string;
  mimeType: string;
  size: number;
  takenAt?: Date | undefined;
  location?: GeoPoint | undefined;
  caption: string;
  audit?: AuditTimestamps | undefined;
}

export interface GetUploadUrlRequest {
  tripId: string;
  filename: string;
  mimeType: string;
  size: number;
}

export interface GetUploadUrlResponse {
  /** presigned PUT URL */
  uploadUrl: string;
  s3Key: string;
  expiresAt?:
    | Date
    | undefined;
  /** pre-allocated id */
  mediaId: string;
}

export interface ConfirmUploadRequest {
  mediaId: string;
  tripId: string;
  s3Key: string;
  dayId: string;
  scheduleId: string;
  caption: string;
}

export interface ConfirmUploadResponse {
  media?: Media | undefined;
}

export interface ListMediaRequest {
  tripId: string;
  dayId: string;
  page?: PageRequest | undefined;
}

export interface ListMediaResponse {
  items: Media[];
  page?: PageInfo | undefined;
}

export interface DeleteMediaRequest {
  mediaId: string;
}

export interface DeleteMediaResponse {
}

export interface GetMediaUrlRequest {
  mediaId: string;
  thumbnail: boolean;
}

export interface GetMediaUrlResponse {
  url: string;
  expiresAt?: Date | undefined;
}

function createBaseMedia(): Media {
  return {
    id: "",
    tripId: "",
    dayId: "",
    scheduleId: "",
    s3Key: "",
    thumbnailS3Key: "",
    mimeType: "",
    size: 0,
    takenAt: undefined,
    location: undefined,
    caption: "",
    audit: undefined,
  };
}

export const Media: MessageFns<Media> = {
  encode(message: Media, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.tripId !== "") {
      writer.uint32(18).string(message.tripId);
    }
    if (message.dayId !== "") {
      writer.uint32(26).string(message.dayId);
    }
    if (message.scheduleId !== "") {
      writer.uint32(34).string(message.scheduleId);
    }
    if (message.s3Key !== "") {
      writer.uint32(42).string(message.s3Key);
    }
    if (message.thumbnailS3Key !== "") {
      writer.uint32(50).string(message.thumbnailS3Key);
    }
    if (message.mimeType !== "") {
      writer.uint32(58).string(message.mimeType);
    }
    if (message.size !== 0) {
      writer.uint32(64).int64(message.size);
    }
    if (message.takenAt !== undefined) {
      Timestamp.encode(toTimestamp(message.takenAt), writer.uint32(74).fork()).join();
    }
    if (message.location !== undefined) {
      GeoPoint.encode(message.location, writer.uint32(82).fork()).join();
    }
    if (message.caption !== "") {
      writer.uint32(90).string(message.caption);
    }
    if (message.audit !== undefined) {
      AuditTimestamps.encode(message.audit, writer.uint32(98).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Media {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMedia();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.tripId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.dayId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.scheduleId = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.s3Key = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.thumbnailS3Key = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.mimeType = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.size = longToNumber(reader.int64());
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.takenAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.location = GeoPoint.decode(reader, reader.uint32());
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.caption = reader.string();
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.audit = AuditTimestamps.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Media {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      tripId: isSet(object.tripId)
        ? globalThis.String(object.tripId)
        : isSet(object.trip_id)
        ? globalThis.String(object.trip_id)
        : "",
      dayId: isSet(object.dayId)
        ? globalThis.String(object.dayId)
        : isSet(object.day_id)
        ? globalThis.String(object.day_id)
        : "",
      scheduleId: isSet(object.scheduleId)
        ? globalThis.String(object.scheduleId)
        : isSet(object.schedule_id)
        ? globalThis.String(object.schedule_id)
        : "",
      s3Key: isSet(object.s3Key)
        ? globalThis.String(object.s3Key)
        : isSet(object.s3_key)
        ? globalThis.String(object.s3_key)
        : "",
      thumbnailS3Key: isSet(object.thumbnailS3Key)
        ? globalThis.String(object.thumbnailS3Key)
        : isSet(object.thumbnail_s3_key)
        ? globalThis.String(object.thumbnail_s3_key)
        : "",
      mimeType: isSet(object.mimeType)
        ? globalThis.String(object.mimeType)
        : isSet(object.mime_type)
        ? globalThis.String(object.mime_type)
        : "",
      size: isSet(object.size) ? globalThis.Number(object.size) : 0,
      takenAt: isSet(object.takenAt)
        ? fromJsonTimestamp(object.takenAt)
        : isSet(object.taken_at)
        ? fromJsonTimestamp(object.taken_at)
        : undefined,
      location: isSet(object.location) ? GeoPoint.fromJSON(object.location) : undefined,
      caption: isSet(object.caption) ? globalThis.String(object.caption) : "",
      audit: isSet(object.audit) ? AuditTimestamps.fromJSON(object.audit) : undefined,
    };
  },

  toJSON(message: Media): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.tripId !== "") {
      obj.tripId = message.tripId;
    }
    if (message.dayId !== "") {
      obj.dayId = message.dayId;
    }
    if (message.scheduleId !== "") {
      obj.scheduleId = message.scheduleId;
    }
    if (message.s3Key !== "") {
      obj.s3Key = message.s3Key;
    }
    if (message.thumbnailS3Key !== "") {
      obj.thumbnailS3Key = message.thumbnailS3Key;
    }
    if (message.mimeType !== "") {
      obj.mimeType = message.mimeType;
    }
    if (message.size !== 0) {
      obj.size = Math.round(message.size);
    }
    if (message.takenAt !== undefined) {
      obj.takenAt = message.takenAt.toISOString();
    }
    if (message.location !== undefined) {
      obj.location = GeoPoint.toJSON(message.location);
    }
    if (message.caption !== "") {
      obj.caption = message.caption;
    }
    if (message.audit !== undefined) {
      obj.audit = AuditTimestamps.toJSON(message.audit);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Media>, I>>(base?: I): Media {
    return Media.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Media>, I>>(object: I): Media {
    const message = createBaseMedia();
    message.id = object.id ?? "";
    message.tripId = object.tripId ?? "";
    message.dayId = object.dayId ?? "";
    message.scheduleId = object.scheduleId ?? "";
    message.s3Key = object.s3Key ?? "";
    message.thumbnailS3Key = object.thumbnailS3Key ?? "";
    message.mimeType = object.mimeType ?? "";
    message.size = object.size ?? 0;
    message.takenAt = object.takenAt ?? undefined;
    message.location = (object.location !== undefined && object.location !== null)
      ? GeoPoint.fromPartial(object.location)
      : undefined;
    message.caption = object.caption ?? "";
    message.audit = (object.audit !== undefined && object.audit !== null)
      ? AuditTimestamps.fromPartial(object.audit)
      : undefined;
    return message;
  },
};

function createBaseGetUploadUrlRequest(): GetUploadUrlRequest {
  return { tripId: "", filename: "", mimeType: "", size: 0 };
}

export const GetUploadUrlRequest: MessageFns<GetUploadUrlRequest> = {
  encode(message: GetUploadUrlRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.tripId !== "") {
      writer.uint32(10).string(message.tripId);
    }
    if (message.filename !== "") {
      writer.uint32(18).string(message.filename);
    }
    if (message.mimeType !== "") {
      writer.uint32(26).string(message.mimeType);
    }
    if (message.size !== 0) {
      writer.uint32(32).int64(message.size);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetUploadUrlRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetUploadUrlRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.tripId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.filename = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.mimeType = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.size = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetUploadUrlRequest {
    return {
      tripId: isSet(object.tripId)
        ? globalThis.String(object.tripId)
        : isSet(object.trip_id)
        ? globalThis.String(object.trip_id)
        : "",
      filename: isSet(object.filename) ? globalThis.String(object.filename) : "",
      mimeType: isSet(object.mimeType)
        ? globalThis.String(object.mimeType)
        : isSet(object.mime_type)
        ? globalThis.String(object.mime_type)
        : "",
      size: isSet(object.size) ? globalThis.Number(object.size) : 0,
    };
  },

  toJSON(message: GetUploadUrlRequest): unknown {
    const obj: any = {};
    if (message.tripId !== "") {
      obj.tripId = message.tripId;
    }
    if (message.filename !== "") {
      obj.filename = message.filename;
    }
    if (message.mimeType !== "") {
      obj.mimeType = message.mimeType;
    }
    if (message.size !== 0) {
      obj.size = Math.round(message.size);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetUploadUrlRequest>, I>>(base?: I): GetUploadUrlRequest {
    return GetUploadUrlRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetUploadUrlRequest>, I>>(object: I): GetUploadUrlRequest {
    const message = createBaseGetUploadUrlRequest();
    message.tripId = object.tripId ?? "";
    message.filename = object.filename ?? "";
    message.mimeType = object.mimeType ?? "";
    message.size = object.size ?? 0;
    return message;
  },
};

function createBaseGetUploadUrlResponse(): GetUploadUrlResponse {
  return { uploadUrl: "", s3Key: "", expiresAt: undefined, mediaId: "" };
}

export const GetUploadUrlResponse: MessageFns<GetUploadUrlResponse> = {
  encode(message: GetUploadUrlResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.uploadUrl !== "") {
      writer.uint32(10).string(message.uploadUrl);
    }
    if (message.s3Key !== "") {
      writer.uint32(18).string(message.s3Key);
    }
    if (message.expiresAt !== undefined) {
      Timestamp.encode(toTimestamp(message.expiresAt), writer.uint32(26).fork()).join();
    }
    if (message.mediaId !== "") {
      writer.uint32(34).string(message.mediaId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetUploadUrlResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetUploadUrlResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.uploadUrl = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.s3Key = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.expiresAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.mediaId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetUploadUrlResponse {
    return {
      uploadUrl: isSet(object.uploadUrl)
        ? globalThis.String(object.uploadUrl)
        : isSet(object.upload_url)
        ? globalThis.String(object.upload_url)
        : "",
      s3Key: isSet(object.s3Key)
        ? globalThis.String(object.s3Key)
        : isSet(object.s3_key)
        ? globalThis.String(object.s3_key)
        : "",
      expiresAt: isSet(object.expiresAt)
        ? fromJsonTimestamp(object.expiresAt)
        : isSet(object.expires_at)
        ? fromJsonTimestamp(object.expires_at)
        : undefined,
      mediaId: isSet(object.mediaId)
        ? globalThis.String(object.mediaId)
        : isSet(object.media_id)
        ? globalThis.String(object.media_id)
        : "",
    };
  },

  toJSON(message: GetUploadUrlResponse): unknown {
    const obj: any = {};
    if (message.uploadUrl !== "") {
      obj.uploadUrl = message.uploadUrl;
    }
    if (message.s3Key !== "") {
      obj.s3Key = message.s3Key;
    }
    if (message.expiresAt !== undefined) {
      obj.expiresAt = message.expiresAt.toISOString();
    }
    if (message.mediaId !== "") {
      obj.mediaId = message.mediaId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetUploadUrlResponse>, I>>(base?: I): GetUploadUrlResponse {
    return GetUploadUrlResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetUploadUrlResponse>, I>>(object: I): GetUploadUrlResponse {
    const message = createBaseGetUploadUrlResponse();
    message.uploadUrl = object.uploadUrl ?? "";
    message.s3Key = object.s3Key ?? "";
    message.expiresAt = object.expiresAt ?? undefined;
    message.mediaId = object.mediaId ?? "";
    return message;
  },
};

function createBaseConfirmUploadRequest(): ConfirmUploadRequest {
  return { mediaId: "", tripId: "", s3Key: "", dayId: "", scheduleId: "", caption: "" };
}

export const ConfirmUploadRequest: MessageFns<ConfirmUploadRequest> = {
  encode(message: ConfirmUploadRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.mediaId !== "") {
      writer.uint32(10).string(message.mediaId);
    }
    if (message.tripId !== "") {
      writer.uint32(18).string(message.tripId);
    }
    if (message.s3Key !== "") {
      writer.uint32(26).string(message.s3Key);
    }
    if (message.dayId !== "") {
      writer.uint32(34).string(message.dayId);
    }
    if (message.scheduleId !== "") {
      writer.uint32(42).string(message.scheduleId);
    }
    if (message.caption !== "") {
      writer.uint32(50).string(message.caption);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ConfirmUploadRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseConfirmUploadRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.mediaId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.tripId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.s3Key = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.dayId = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.scheduleId = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.caption = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ConfirmUploadRequest {
    return {
      mediaId: isSet(object.mediaId)
        ? globalThis.String(object.mediaId)
        : isSet(object.media_id)
        ? globalThis.String(object.media_id)
        : "",
      tripId: isSet(object.tripId)
        ? globalThis.String(object.tripId)
        : isSet(object.trip_id)
        ? globalThis.String(object.trip_id)
        : "",
      s3Key: isSet(object.s3Key)
        ? globalThis.String(object.s3Key)
        : isSet(object.s3_key)
        ? globalThis.String(object.s3_key)
        : "",
      dayId: isSet(object.dayId)
        ? globalThis.String(object.dayId)
        : isSet(object.day_id)
        ? globalThis.String(object.day_id)
        : "",
      scheduleId: isSet(object.scheduleId)
        ? globalThis.String(object.scheduleId)
        : isSet(object.schedule_id)
        ? globalThis.String(object.schedule_id)
        : "",
      caption: isSet(object.caption) ? globalThis.String(object.caption) : "",
    };
  },

  toJSON(message: ConfirmUploadRequest): unknown {
    const obj: any = {};
    if (message.mediaId !== "") {
      obj.mediaId = message.mediaId;
    }
    if (message.tripId !== "") {
      obj.tripId = message.tripId;
    }
    if (message.s3Key !== "") {
      obj.s3Key = message.s3Key;
    }
    if (message.dayId !== "") {
      obj.dayId = message.dayId;
    }
    if (message.scheduleId !== "") {
      obj.scheduleId = message.scheduleId;
    }
    if (message.caption !== "") {
      obj.caption = message.caption;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ConfirmUploadRequest>, I>>(base?: I): ConfirmUploadRequest {
    return ConfirmUploadRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ConfirmUploadRequest>, I>>(object: I): ConfirmUploadRequest {
    const message = createBaseConfirmUploadRequest();
    message.mediaId = object.mediaId ?? "";
    message.tripId = object.tripId ?? "";
    message.s3Key = object.s3Key ?? "";
    message.dayId = object.dayId ?? "";
    message.scheduleId = object.scheduleId ?? "";
    message.caption = object.caption ?? "";
    return message;
  },
};

function createBaseConfirmUploadResponse(): ConfirmUploadResponse {
  return { media: undefined };
}

export const ConfirmUploadResponse: MessageFns<ConfirmUploadResponse> = {
  encode(message: ConfirmUploadResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.media !== undefined) {
      Media.encode(message.media, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ConfirmUploadResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseConfirmUploadResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.media = Media.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ConfirmUploadResponse {
    return { media: isSet(object.media) ? Media.fromJSON(object.media) : undefined };
  },

  toJSON(message: ConfirmUploadResponse): unknown {
    const obj: any = {};
    if (message.media !== undefined) {
      obj.media = Media.toJSON(message.media);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ConfirmUploadResponse>, I>>(base?: I): ConfirmUploadResponse {
    return ConfirmUploadResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ConfirmUploadResponse>, I>>(object: I): ConfirmUploadResponse {
    const message = createBaseConfirmUploadResponse();
    message.media = (object.media !== undefined && object.media !== null) ? Media.fromPartial(object.media) : undefined;
    return message;
  },
};

function createBaseListMediaRequest(): ListMediaRequest {
  return { tripId: "", dayId: "", page: undefined };
}

export const ListMediaRequest: MessageFns<ListMediaRequest> = {
  encode(message: ListMediaRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.tripId !== "") {
      writer.uint32(10).string(message.tripId);
    }
    if (message.dayId !== "") {
      writer.uint32(18).string(message.dayId);
    }
    if (message.page !== undefined) {
      PageRequest.encode(message.page, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListMediaRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListMediaRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.tripId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.dayId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.page = PageRequest.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListMediaRequest {
    return {
      tripId: isSet(object.tripId)
        ? globalThis.String(object.tripId)
        : isSet(object.trip_id)
        ? globalThis.String(object.trip_id)
        : "",
      dayId: isSet(object.dayId)
        ? globalThis.String(object.dayId)
        : isSet(object.day_id)
        ? globalThis.String(object.day_id)
        : "",
      page: isSet(object.page) ? PageRequest.fromJSON(object.page) : undefined,
    };
  },

  toJSON(message: ListMediaRequest): unknown {
    const obj: any = {};
    if (message.tripId !== "") {
      obj.tripId = message.tripId;
    }
    if (message.dayId !== "") {
      obj.dayId = message.dayId;
    }
    if (message.page !== undefined) {
      obj.page = PageRequest.toJSON(message.page);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListMediaRequest>, I>>(base?: I): ListMediaRequest {
    return ListMediaRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListMediaRequest>, I>>(object: I): ListMediaRequest {
    const message = createBaseListMediaRequest();
    message.tripId = object.tripId ?? "";
    message.dayId = object.dayId ?? "";
    message.page = (object.page !== undefined && object.page !== null)
      ? PageRequest.fromPartial(object.page)
      : undefined;
    return message;
  },
};

function createBaseListMediaResponse(): ListMediaResponse {
  return { items: [], page: undefined };
}

export const ListMediaResponse: MessageFns<ListMediaResponse> = {
  encode(message: ListMediaResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.items) {
      Media.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.page !== undefined) {
      PageInfo.encode(message.page, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListMediaResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListMediaResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.items.push(Media.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.page = PageInfo.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListMediaResponse {
    return {
      items: globalThis.Array.isArray(object?.items) ? object.items.map((e: any) => Media.fromJSON(e)) : [],
      page: isSet(object.page) ? PageInfo.fromJSON(object.page) : undefined,
    };
  },

  toJSON(message: ListMediaResponse): unknown {
    const obj: any = {};
    if (message.items?.length) {
      obj.items = message.items.map((e) => Media.toJSON(e));
    }
    if (message.page !== undefined) {
      obj.page = PageInfo.toJSON(message.page);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListMediaResponse>, I>>(base?: I): ListMediaResponse {
    return ListMediaResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListMediaResponse>, I>>(object: I): ListMediaResponse {
    const message = createBaseListMediaResponse();
    message.items = object.items?.map((e) => Media.fromPartial(e)) || [];
    message.page = (object.page !== undefined && object.page !== null) ? PageInfo.fromPartial(object.page) : undefined;
    return message;
  },
};

function createBaseDeleteMediaRequest(): DeleteMediaRequest {
  return { mediaId: "" };
}

export const DeleteMediaRequest: MessageFns<DeleteMediaRequest> = {
  encode(message: DeleteMediaRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.mediaId !== "") {
      writer.uint32(10).string(message.mediaId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeleteMediaRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeleteMediaRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.mediaId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeleteMediaRequest {
    return {
      mediaId: isSet(object.mediaId)
        ? globalThis.String(object.mediaId)
        : isSet(object.media_id)
        ? globalThis.String(object.media_id)
        : "",
    };
  },

  toJSON(message: DeleteMediaRequest): unknown {
    const obj: any = {};
    if (message.mediaId !== "") {
      obj.mediaId = message.mediaId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DeleteMediaRequest>, I>>(base?: I): DeleteMediaRequest {
    return DeleteMediaRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeleteMediaRequest>, I>>(object: I): DeleteMediaRequest {
    const message = createBaseDeleteMediaRequest();
    message.mediaId = object.mediaId ?? "";
    return message;
  },
};

function createBaseDeleteMediaResponse(): DeleteMediaResponse {
  return {};
}

export const DeleteMediaResponse: MessageFns<DeleteMediaResponse> = {
  encode(_: DeleteMediaResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeleteMediaResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeleteMediaResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): DeleteMediaResponse {
    return {};
  },

  toJSON(_: DeleteMediaResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<DeleteMediaResponse>, I>>(base?: I): DeleteMediaResponse {
    return DeleteMediaResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeleteMediaResponse>, I>>(_: I): DeleteMediaResponse {
    const message = createBaseDeleteMediaResponse();
    return message;
  },
};

function createBaseGetMediaUrlRequest(): GetMediaUrlRequest {
  return { mediaId: "", thumbnail: false };
}

export const GetMediaUrlRequest: MessageFns<GetMediaUrlRequest> = {
  encode(message: GetMediaUrlRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.mediaId !== "") {
      writer.uint32(10).string(message.mediaId);
    }
    if (message.thumbnail !== false) {
      writer.uint32(16).bool(message.thumbnail);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetMediaUrlRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetMediaUrlRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.mediaId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.thumbnail = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetMediaUrlRequest {
    return {
      mediaId: isSet(object.mediaId)
        ? globalThis.String(object.mediaId)
        : isSet(object.media_id)
        ? globalThis.String(object.media_id)
        : "",
      thumbnail: isSet(object.thumbnail) ? globalThis.Boolean(object.thumbnail) : false,
    };
  },

  toJSON(message: GetMediaUrlRequest): unknown {
    const obj: any = {};
    if (message.mediaId !== "") {
      obj.mediaId = message.mediaId;
    }
    if (message.thumbnail !== false) {
      obj.thumbnail = message.thumbnail;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetMediaUrlRequest>, I>>(base?: I): GetMediaUrlRequest {
    return GetMediaUrlRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetMediaUrlRequest>, I>>(object: I): GetMediaUrlRequest {
    const message = createBaseGetMediaUrlRequest();
    message.mediaId = object.mediaId ?? "";
    message.thumbnail = object.thumbnail ?? false;
    return message;
  },
};

function createBaseGetMediaUrlResponse(): GetMediaUrlResponse {
  return { url: "", expiresAt: undefined };
}

export const GetMediaUrlResponse: MessageFns<GetMediaUrlResponse> = {
  encode(message: GetMediaUrlResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.url !== "") {
      writer.uint32(10).string(message.url);
    }
    if (message.expiresAt !== undefined) {
      Timestamp.encode(toTimestamp(message.expiresAt), writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetMediaUrlResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetMediaUrlResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.url = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.expiresAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetMediaUrlResponse {
    return {
      url: isSet(object.url) ? globalThis.String(object.url) : "",
      expiresAt: isSet(object.expiresAt)
        ? fromJsonTimestamp(object.expiresAt)
        : isSet(object.expires_at)
        ? fromJsonTimestamp(object.expires_at)
        : undefined,
    };
  },

  toJSON(message: GetMediaUrlResponse): unknown {
    const obj: any = {};
    if (message.url !== "") {
      obj.url = message.url;
    }
    if (message.expiresAt !== undefined) {
      obj.expiresAt = message.expiresAt.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetMediaUrlResponse>, I>>(base?: I): GetMediaUrlResponse {
    return GetMediaUrlResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetMediaUrlResponse>, I>>(object: I): GetMediaUrlResponse {
    const message = createBaseGetMediaUrlResponse();
    message.url = object.url ?? "";
    message.expiresAt = object.expiresAt ?? undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}

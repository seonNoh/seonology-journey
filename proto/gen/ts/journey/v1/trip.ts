// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               unknown
// source: journey/v1/trip.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import {
  AuditTimestamps,
  Money,
  PageInfo,
  PageRequest,
  TripStatus,
  tripStatusFromJSON,
  tripStatusToJSON,
} from "./common";

export const protobufPackage = "journey.v1";

/** Trip - 여행 전체 */
export interface Trip {
  id: string;
  ownerId: string;
  title: string;
  description: string;
  /** YYYY-MM-DD */
  startDate: string;
  /** YYYY-MM-DD */
  endDate: string;
  status: TripStatus;
  coverImageUrl: string;
  totalBudget?: Money | undefined;
  destination: string;
  /** ISO 3166-1 alpha-2 (JP, KR, ...) */
  countryCode: string;
  audit?: AuditTimestamps | undefined;
}

/** Day - 일차 */
export interface Day {
  id: string;
  tripId: string;
  dayNumber: number;
  /** YYYY-MM-DD */
  date: string;
  /** 월/화/... */
  dayOfWeek: string;
  region: string;
  weather: string;
  dailySummary: string;
  audit?: AuditTimestamps | undefined;
}

export interface CreateTripRequest {
  title: string;
  description: string;
  startDate: string;
  endDate: string;
  destination: string;
  countryCode: string;
  totalBudget?: Money | undefined;
}

export interface CreateTripResponse {
  trip?: Trip | undefined;
}

export interface GetTripRequest {
  tripId: string;
}

export interface GetTripResponse {
  trip?: Trip | undefined;
}

export interface ListTripsRequest {
  /** unspecified = all */
  status: TripStatus;
  page?: PageRequest | undefined;
}

export interface ListTripsResponse {
  trips: Trip[];
  page?: PageInfo | undefined;
}

export interface UpdateTripRequest {
  tripId: string;
  title: string;
  description: string;
  startDate: string;
  endDate: string;
  status: TripStatus;
  coverImageUrl: string;
  totalBudget?: Money | undefined;
  destination: string;
  countryCode: string;
}

export interface UpdateTripResponse {
  trip?: Trip | undefined;
}

export interface DeleteTripRequest {
  tripId: string;
}

export interface DeleteTripResponse {
}

export interface ListDaysRequest {
  tripId: string;
}

export interface ListDaysResponse {
  days: Day[];
}

export interface UpdateDayRequest {
  dayId: string;
  region: string;
  weather: string;
  dailySummary: string;
}

export interface UpdateDayResponse {
  day?: Day | undefined;
}

function createBaseTrip(): Trip {
  return {
    id: "",
    ownerId: "",
    title: "",
    description: "",
    startDate: "",
    endDate: "",
    status: 0,
    coverImageUrl: "",
    totalBudget: undefined,
    destination: "",
    countryCode: "",
    audit: undefined,
  };
}

export const Trip: MessageFns<Trip> = {
  encode(message: Trip, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.ownerId !== "") {
      writer.uint32(18).string(message.ownerId);
    }
    if (message.title !== "") {
      writer.uint32(26).string(message.title);
    }
    if (message.description !== "") {
      writer.uint32(34).string(message.description);
    }
    if (message.startDate !== "") {
      writer.uint32(42).string(message.startDate);
    }
    if (message.endDate !== "") {
      writer.uint32(50).string(message.endDate);
    }
    if (message.status !== 0) {
      writer.uint32(56).int32(message.status);
    }
    if (message.coverImageUrl !== "") {
      writer.uint32(66).string(message.coverImageUrl);
    }
    if (message.totalBudget !== undefined) {
      Money.encode(message.totalBudget, writer.uint32(74).fork()).join();
    }
    if (message.destination !== "") {
      writer.uint32(82).string(message.destination);
    }
    if (message.countryCode !== "") {
      writer.uint32(90).string(message.countryCode);
    }
    if (message.audit !== undefined) {
      AuditTimestamps.encode(message.audit, writer.uint32(98).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Trip {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTrip();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.ownerId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.title = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.startDate = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.endDate = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.coverImageUrl = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.totalBudget = Money.decode(reader, reader.uint32());
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.destination = reader.string();
          continue;
        }
        case 11: {
          if (tag !== 90) {
            break;
          }

          message.countryCode = reader.string();
          continue;
        }
        case 12: {
          if (tag !== 98) {
            break;
          }

          message.audit = AuditTimestamps.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Trip {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      ownerId: isSet(object.ownerId)
        ? globalThis.String(object.ownerId)
        : isSet(object.owner_id)
        ? globalThis.String(object.owner_id)
        : "",
      title: isSet(object.title) ? globalThis.String(object.title) : "",
      description: isSet(object.description) ? globalThis.String(object.description) : "",
      startDate: isSet(object.startDate)
        ? globalThis.String(object.startDate)
        : isSet(object.start_date)
        ? globalThis.String(object.start_date)
        : "",
      endDate: isSet(object.endDate)
        ? globalThis.String(object.endDate)
        : isSet(object.end_date)
        ? globalThis.String(object.end_date)
        : "",
      status: isSet(object.status) ? tripStatusFromJSON(object.status) : 0,
      coverImageUrl: isSet(object.coverImageUrl)
        ? globalThis.String(object.coverImageUrl)
        : isSet(object.cover_image_url)
        ? globalThis.String(object.cover_image_url)
        : "",
      totalBudget: isSet(object.totalBudget)
        ? Money.fromJSON(object.totalBudget)
        : isSet(object.total_budget)
        ? Money.fromJSON(object.total_budget)
        : undefined,
      destination: isSet(object.destination) ? globalThis.String(object.destination) : "",
      countryCode: isSet(object.countryCode)
        ? globalThis.String(object.countryCode)
        : isSet(object.country_code)
        ? globalThis.String(object.country_code)
        : "",
      audit: isSet(object.audit) ? AuditTimestamps.fromJSON(object.audit) : undefined,
    };
  },

  toJSON(message: Trip): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.ownerId !== "") {
      obj.ownerId = message.ownerId;
    }
    if (message.title !== "") {
      obj.title = message.title;
    }
    if (message.description !== "") {
      obj.description = message.description;
    }
    if (message.startDate !== "") {
      obj.startDate = message.startDate;
    }
    if (message.endDate !== "") {
      obj.endDate = message.endDate;
    }
    if (message.status !== 0) {
      obj.status = tripStatusToJSON(message.status);
    }
    if (message.coverImageUrl !== "") {
      obj.coverImageUrl = message.coverImageUrl;
    }
    if (message.totalBudget !== undefined) {
      obj.totalBudget = Money.toJSON(message.totalBudget);
    }
    if (message.destination !== "") {
      obj.destination = message.destination;
    }
    if (message.countryCode !== "") {
      obj.countryCode = message.countryCode;
    }
    if (message.audit !== undefined) {
      obj.audit = AuditTimestamps.toJSON(message.audit);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Trip>, I>>(base?: I): Trip {
    return Trip.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Trip>, I>>(object: I): Trip {
    const message = createBaseTrip();
    message.id = object.id ?? "";
    message.ownerId = object.ownerId ?? "";
    message.title = object.title ?? "";
    message.description = object.description ?? "";
    message.startDate = object.startDate ?? "";
    message.endDate = object.endDate ?? "";
    message.status = object.status ?? 0;
    message.coverImageUrl = object.coverImageUrl ?? "";
    message.totalBudget = (object.totalBudget !== undefined && object.totalBudget !== null)
      ? Money.fromPartial(object.totalBudget)
      : undefined;
    message.destination = object.destination ?? "";
    message.countryCode = object.countryCode ?? "";
    message.audit = (object.audit !== undefined && object.audit !== null)
      ? AuditTimestamps.fromPartial(object.audit)
      : undefined;
    return message;
  },
};

function createBaseDay(): Day {
  return {
    id: "",
    tripId: "",
    dayNumber: 0,
    date: "",
    dayOfWeek: "",
    region: "",
    weather: "",
    dailySummary: "",
    audit: undefined,
  };
}

export const Day: MessageFns<Day> = {
  encode(message: Day, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.tripId !== "") {
      writer.uint32(18).string(message.tripId);
    }
    if (message.dayNumber !== 0) {
      writer.uint32(24).int32(message.dayNumber);
    }
    if (message.date !== "") {
      writer.uint32(34).string(message.date);
    }
    if (message.dayOfWeek !== "") {
      writer.uint32(42).string(message.dayOfWeek);
    }
    if (message.region !== "") {
      writer.uint32(50).string(message.region);
    }
    if (message.weather !== "") {
      writer.uint32(58).string(message.weather);
    }
    if (message.dailySummary !== "") {
      writer.uint32(66).string(message.dailySummary);
    }
    if (message.audit !== undefined) {
      AuditTimestamps.encode(message.audit, writer.uint32(74).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Day {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDay();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.tripId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.dayNumber = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.date = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.dayOfWeek = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.region = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.weather = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.dailySummary = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.audit = AuditTimestamps.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Day {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      tripId: isSet(object.tripId)
        ? globalThis.String(object.tripId)
        : isSet(object.trip_id)
        ? globalThis.String(object.trip_id)
        : "",
      dayNumber: isSet(object.dayNumber)
        ? globalThis.Number(object.dayNumber)
        : isSet(object.day_number)
        ? globalThis.Number(object.day_number)
        : 0,
      date: isSet(object.date) ? globalThis.String(object.date) : "",
      dayOfWeek: isSet(object.dayOfWeek)
        ? globalThis.String(object.dayOfWeek)
        : isSet(object.day_of_week)
        ? globalThis.String(object.day_of_week)
        : "",
      region: isSet(object.region) ? globalThis.String(object.region) : "",
      weather: isSet(object.weather) ? globalThis.String(object.weather) : "",
      dailySummary: isSet(object.dailySummary)
        ? globalThis.String(object.dailySummary)
        : isSet(object.daily_summary)
        ? globalThis.String(object.daily_summary)
        : "",
      audit: isSet(object.audit) ? AuditTimestamps.fromJSON(object.audit) : undefined,
    };
  },

  toJSON(message: Day): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.tripId !== "") {
      obj.tripId = message.tripId;
    }
    if (message.dayNumber !== 0) {
      obj.dayNumber = Math.round(message.dayNumber);
    }
    if (message.date !== "") {
      obj.date = message.date;
    }
    if (message.dayOfWeek !== "") {
      obj.dayOfWeek = message.dayOfWeek;
    }
    if (message.region !== "") {
      obj.region = message.region;
    }
    if (message.weather !== "") {
      obj.weather = message.weather;
    }
    if (message.dailySummary !== "") {
      obj.dailySummary = message.dailySummary;
    }
    if (message.audit !== undefined) {
      obj.audit = AuditTimestamps.toJSON(message.audit);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Day>, I>>(base?: I): Day {
    return Day.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Day>, I>>(object: I): Day {
    const message = createBaseDay();
    message.id = object.id ?? "";
    message.tripId = object.tripId ?? "";
    message.dayNumber = object.dayNumber ?? 0;
    message.date = object.date ?? "";
    message.dayOfWeek = object.dayOfWeek ?? "";
    message.region = object.region ?? "";
    message.weather = object.weather ?? "";
    message.dailySummary = object.dailySummary ?? "";
    message.audit = (object.audit !== undefined && object.audit !== null)
      ? AuditTimestamps.fromPartial(object.audit)
      : undefined;
    return message;
  },
};

function createBaseCreateTripRequest(): CreateTripRequest {
  return {
    title: "",
    description: "",
    startDate: "",
    endDate: "",
    destination: "",
    countryCode: "",
    totalBudget: undefined,
  };
}

export const CreateTripRequest: MessageFns<CreateTripRequest> = {
  encode(message: CreateTripRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.title !== "") {
      writer.uint32(10).string(message.title);
    }
    if (message.description !== "") {
      writer.uint32(18).string(message.description);
    }
    if (message.startDate !== "") {
      writer.uint32(26).string(message.startDate);
    }
    if (message.endDate !== "") {
      writer.uint32(34).string(message.endDate);
    }
    if (message.destination !== "") {
      writer.uint32(42).string(message.destination);
    }
    if (message.countryCode !== "") {
      writer.uint32(50).string(message.countryCode);
    }
    if (message.totalBudget !== undefined) {
      Money.encode(message.totalBudget, writer.uint32(58).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateTripRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateTripRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.title = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.startDate = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.endDate = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.destination = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.countryCode = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.totalBudget = Money.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateTripRequest {
    return {
      title: isSet(object.title) ? globalThis.String(object.title) : "",
      description: isSet(object.description) ? globalThis.String(object.description) : "",
      startDate: isSet(object.startDate)
        ? globalThis.String(object.startDate)
        : isSet(object.start_date)
        ? globalThis.String(object.start_date)
        : "",
      endDate: isSet(object.endDate)
        ? globalThis.String(object.endDate)
        : isSet(object.end_date)
        ? globalThis.String(object.end_date)
        : "",
      destination: isSet(object.destination) ? globalThis.String(object.destination) : "",
      countryCode: isSet(object.countryCode)
        ? globalThis.String(object.countryCode)
        : isSet(object.country_code)
        ? globalThis.String(object.country_code)
        : "",
      totalBudget: isSet(object.totalBudget)
        ? Money.fromJSON(object.totalBudget)
        : isSet(object.total_budget)
        ? Money.fromJSON(object.total_budget)
        : undefined,
    };
  },

  toJSON(message: CreateTripRequest): unknown {
    const obj: any = {};
    if (message.title !== "") {
      obj.title = message.title;
    }
    if (message.description !== "") {
      obj.description = message.description;
    }
    if (message.startDate !== "") {
      obj.startDate = message.startDate;
    }
    if (message.endDate !== "") {
      obj.endDate = message.endDate;
    }
    if (message.destination !== "") {
      obj.destination = message.destination;
    }
    if (message.countryCode !== "") {
      obj.countryCode = message.countryCode;
    }
    if (message.totalBudget !== undefined) {
      obj.totalBudget = Money.toJSON(message.totalBudget);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CreateTripRequest>, I>>(base?: I): CreateTripRequest {
    return CreateTripRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CreateTripRequest>, I>>(object: I): CreateTripRequest {
    const message = createBaseCreateTripRequest();
    message.title = object.title ?? "";
    message.description = object.description ?? "";
    message.startDate = object.startDate ?? "";
    message.endDate = object.endDate ?? "";
    message.destination = object.destination ?? "";
    message.countryCode = object.countryCode ?? "";
    message.totalBudget = (object.totalBudget !== undefined && object.totalBudget !== null)
      ? Money.fromPartial(object.totalBudget)
      : undefined;
    return message;
  },
};

function createBaseCreateTripResponse(): CreateTripResponse {
  return { trip: undefined };
}

export const CreateTripResponse: MessageFns<CreateTripResponse> = {
  encode(message: CreateTripResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.trip !== undefined) {
      Trip.encode(message.trip, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateTripResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateTripResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.trip = Trip.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateTripResponse {
    return { trip: isSet(object.trip) ? Trip.fromJSON(object.trip) : undefined };
  },

  toJSON(message: CreateTripResponse): unknown {
    const obj: any = {};
    if (message.trip !== undefined) {
      obj.trip = Trip.toJSON(message.trip);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CreateTripResponse>, I>>(base?: I): CreateTripResponse {
    return CreateTripResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CreateTripResponse>, I>>(object: I): CreateTripResponse {
    const message = createBaseCreateTripResponse();
    message.trip = (object.trip !== undefined && object.trip !== null) ? Trip.fromPartial(object.trip) : undefined;
    return message;
  },
};

function createBaseGetTripRequest(): GetTripRequest {
  return { tripId: "" };
}

export const GetTripRequest: MessageFns<GetTripRequest> = {
  encode(message: GetTripRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.tripId !== "") {
      writer.uint32(10).string(message.tripId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetTripRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetTripRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.tripId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetTripRequest {
    return {
      tripId: isSet(object.tripId)
        ? globalThis.String(object.tripId)
        : isSet(object.trip_id)
        ? globalThis.String(object.trip_id)
        : "",
    };
  },

  toJSON(message: GetTripRequest): unknown {
    const obj: any = {};
    if (message.tripId !== "") {
      obj.tripId = message.tripId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetTripRequest>, I>>(base?: I): GetTripRequest {
    return GetTripRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetTripRequest>, I>>(object: I): GetTripRequest {
    const message = createBaseGetTripRequest();
    message.tripId = object.tripId ?? "";
    return message;
  },
};

function createBaseGetTripResponse(): GetTripResponse {
  return { trip: undefined };
}

export const GetTripResponse: MessageFns<GetTripResponse> = {
  encode(message: GetTripResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.trip !== undefined) {
      Trip.encode(message.trip, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetTripResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetTripResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.trip = Trip.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetTripResponse {
    return { trip: isSet(object.trip) ? Trip.fromJSON(object.trip) : undefined };
  },

  toJSON(message: GetTripResponse): unknown {
    const obj: any = {};
    if (message.trip !== undefined) {
      obj.trip = Trip.toJSON(message.trip);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetTripResponse>, I>>(base?: I): GetTripResponse {
    return GetTripResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetTripResponse>, I>>(object: I): GetTripResponse {
    const message = createBaseGetTripResponse();
    message.trip = (object.trip !== undefined && object.trip !== null) ? Trip.fromPartial(object.trip) : undefined;
    return message;
  },
};

function createBaseListTripsRequest(): ListTripsRequest {
  return { status: 0, page: undefined };
}

export const ListTripsRequest: MessageFns<ListTripsRequest> = {
  encode(message: ListTripsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.status !== 0) {
      writer.uint32(8).int32(message.status);
    }
    if (message.page !== undefined) {
      PageRequest.encode(message.page, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListTripsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListTripsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.page = PageRequest.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListTripsRequest {
    return {
      status: isSet(object.status) ? tripStatusFromJSON(object.status) : 0,
      page: isSet(object.page) ? PageRequest.fromJSON(object.page) : undefined,
    };
  },

  toJSON(message: ListTripsRequest): unknown {
    const obj: any = {};
    if (message.status !== 0) {
      obj.status = tripStatusToJSON(message.status);
    }
    if (message.page !== undefined) {
      obj.page = PageRequest.toJSON(message.page);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListTripsRequest>, I>>(base?: I): ListTripsRequest {
    return ListTripsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListTripsRequest>, I>>(object: I): ListTripsRequest {
    const message = createBaseListTripsRequest();
    message.status = object.status ?? 0;
    message.page = (object.page !== undefined && object.page !== null)
      ? PageRequest.fromPartial(object.page)
      : undefined;
    return message;
  },
};

function createBaseListTripsResponse(): ListTripsResponse {
  return { trips: [], page: undefined };
}

export const ListTripsResponse: MessageFns<ListTripsResponse> = {
  encode(message: ListTripsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.trips) {
      Trip.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.page !== undefined) {
      PageInfo.encode(message.page, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListTripsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListTripsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.trips.push(Trip.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.page = PageInfo.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListTripsResponse {
    return {
      trips: globalThis.Array.isArray(object?.trips) ? object.trips.map((e: any) => Trip.fromJSON(e)) : [],
      page: isSet(object.page) ? PageInfo.fromJSON(object.page) : undefined,
    };
  },

  toJSON(message: ListTripsResponse): unknown {
    const obj: any = {};
    if (message.trips?.length) {
      obj.trips = message.trips.map((e) => Trip.toJSON(e));
    }
    if (message.page !== undefined) {
      obj.page = PageInfo.toJSON(message.page);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListTripsResponse>, I>>(base?: I): ListTripsResponse {
    return ListTripsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListTripsResponse>, I>>(object: I): ListTripsResponse {
    const message = createBaseListTripsResponse();
    message.trips = object.trips?.map((e) => Trip.fromPartial(e)) || [];
    message.page = (object.page !== undefined && object.page !== null) ? PageInfo.fromPartial(object.page) : undefined;
    return message;
  },
};

function createBaseUpdateTripRequest(): UpdateTripRequest {
  return {
    tripId: "",
    title: "",
    description: "",
    startDate: "",
    endDate: "",
    status: 0,
    coverImageUrl: "",
    totalBudget: undefined,
    destination: "",
    countryCode: "",
  };
}

export const UpdateTripRequest: MessageFns<UpdateTripRequest> = {
  encode(message: UpdateTripRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.tripId !== "") {
      writer.uint32(10).string(message.tripId);
    }
    if (message.title !== "") {
      writer.uint32(18).string(message.title);
    }
    if (message.description !== "") {
      writer.uint32(26).string(message.description);
    }
    if (message.startDate !== "") {
      writer.uint32(34).string(message.startDate);
    }
    if (message.endDate !== "") {
      writer.uint32(42).string(message.endDate);
    }
    if (message.status !== 0) {
      writer.uint32(48).int32(message.status);
    }
    if (message.coverImageUrl !== "") {
      writer.uint32(58).string(message.coverImageUrl);
    }
    if (message.totalBudget !== undefined) {
      Money.encode(message.totalBudget, writer.uint32(66).fork()).join();
    }
    if (message.destination !== "") {
      writer.uint32(74).string(message.destination);
    }
    if (message.countryCode !== "") {
      writer.uint32(82).string(message.countryCode);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdateTripRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdateTripRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.tripId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.title = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.startDate = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.endDate = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.coverImageUrl = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.totalBudget = Money.decode(reader, reader.uint32());
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.destination = reader.string();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.countryCode = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpdateTripRequest {
    return {
      tripId: isSet(object.tripId)
        ? globalThis.String(object.tripId)
        : isSet(object.trip_id)
        ? globalThis.String(object.trip_id)
        : "",
      title: isSet(object.title) ? globalThis.String(object.title) : "",
      description: isSet(object.description) ? globalThis.String(object.description) : "",
      startDate: isSet(object.startDate)
        ? globalThis.String(object.startDate)
        : isSet(object.start_date)
        ? globalThis.String(object.start_date)
        : "",
      endDate: isSet(object.endDate)
        ? globalThis.String(object.endDate)
        : isSet(object.end_date)
        ? globalThis.String(object.end_date)
        : "",
      status: isSet(object.status) ? tripStatusFromJSON(object.status) : 0,
      coverImageUrl: isSet(object.coverImageUrl)
        ? globalThis.String(object.coverImageUrl)
        : isSet(object.cover_image_url)
        ? globalThis.String(object.cover_image_url)
        : "",
      totalBudget: isSet(object.totalBudget)
        ? Money.fromJSON(object.totalBudget)
        : isSet(object.total_budget)
        ? Money.fromJSON(object.total_budget)
        : undefined,
      destination: isSet(object.destination) ? globalThis.String(object.destination) : "",
      countryCode: isSet(object.countryCode)
        ? globalThis.String(object.countryCode)
        : isSet(object.country_code)
        ? globalThis.String(object.country_code)
        : "",
    };
  },

  toJSON(message: UpdateTripRequest): unknown {
    const obj: any = {};
    if (message.tripId !== "") {
      obj.tripId = message.tripId;
    }
    if (message.title !== "") {
      obj.title = message.title;
    }
    if (message.description !== "") {
      obj.description = message.description;
    }
    if (message.startDate !== "") {
      obj.startDate = message.startDate;
    }
    if (message.endDate !== "") {
      obj.endDate = message.endDate;
    }
    if (message.status !== 0) {
      obj.status = tripStatusToJSON(message.status);
    }
    if (message.coverImageUrl !== "") {
      obj.coverImageUrl = message.coverImageUrl;
    }
    if (message.totalBudget !== undefined) {
      obj.totalBudget = Money.toJSON(message.totalBudget);
    }
    if (message.destination !== "") {
      obj.destination = message.destination;
    }
    if (message.countryCode !== "") {
      obj.countryCode = message.countryCode;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UpdateTripRequest>, I>>(base?: I): UpdateTripRequest {
    return UpdateTripRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UpdateTripRequest>, I>>(object: I): UpdateTripRequest {
    const message = createBaseUpdateTripRequest();
    message.tripId = object.tripId ?? "";
    message.title = object.title ?? "";
    message.description = object.description ?? "";
    message.startDate = object.startDate ?? "";
    message.endDate = object.endDate ?? "";
    message.status = object.status ?? 0;
    message.coverImageUrl = object.coverImageUrl ?? "";
    message.totalBudget = (object.totalBudget !== undefined && object.totalBudget !== null)
      ? Money.fromPartial(object.totalBudget)
      : undefined;
    message.destination = object.destination ?? "";
    message.countryCode = object.countryCode ?? "";
    return message;
  },
};

function createBaseUpdateTripResponse(): UpdateTripResponse {
  return { trip: undefined };
}

export const UpdateTripResponse: MessageFns<UpdateTripResponse> = {
  encode(message: UpdateTripResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.trip !== undefined) {
      Trip.encode(message.trip, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdateTripResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdateTripResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.trip = Trip.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpdateTripResponse {
    return { trip: isSet(object.trip) ? Trip.fromJSON(object.trip) : undefined };
  },

  toJSON(message: UpdateTripResponse): unknown {
    const obj: any = {};
    if (message.trip !== undefined) {
      obj.trip = Trip.toJSON(message.trip);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UpdateTripResponse>, I>>(base?: I): UpdateTripResponse {
    return UpdateTripResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UpdateTripResponse>, I>>(object: I): UpdateTripResponse {
    const message = createBaseUpdateTripResponse();
    message.trip = (object.trip !== undefined && object.trip !== null) ? Trip.fromPartial(object.trip) : undefined;
    return message;
  },
};

function createBaseDeleteTripRequest(): DeleteTripRequest {
  return { tripId: "" };
}

export const DeleteTripRequest: MessageFns<DeleteTripRequest> = {
  encode(message: DeleteTripRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.tripId !== "") {
      writer.uint32(10).string(message.tripId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeleteTripRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeleteTripRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.tripId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeleteTripRequest {
    return {
      tripId: isSet(object.tripId)
        ? globalThis.String(object.tripId)
        : isSet(object.trip_id)
        ? globalThis.String(object.trip_id)
        : "",
    };
  },

  toJSON(message: DeleteTripRequest): unknown {
    const obj: any = {};
    if (message.tripId !== "") {
      obj.tripId = message.tripId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DeleteTripRequest>, I>>(base?: I): DeleteTripRequest {
    return DeleteTripRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeleteTripRequest>, I>>(object: I): DeleteTripRequest {
    const message = createBaseDeleteTripRequest();
    message.tripId = object.tripId ?? "";
    return message;
  },
};

function createBaseDeleteTripResponse(): DeleteTripResponse {
  return {};
}

export const DeleteTripResponse: MessageFns<DeleteTripResponse> = {
  encode(_: DeleteTripResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeleteTripResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeleteTripResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): DeleteTripResponse {
    return {};
  },

  toJSON(_: DeleteTripResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<DeleteTripResponse>, I>>(base?: I): DeleteTripResponse {
    return DeleteTripResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeleteTripResponse>, I>>(_: I): DeleteTripResponse {
    const message = createBaseDeleteTripResponse();
    return message;
  },
};

function createBaseListDaysRequest(): ListDaysRequest {
  return { tripId: "" };
}

export const ListDaysRequest: MessageFns<ListDaysRequest> = {
  encode(message: ListDaysRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.tripId !== "") {
      writer.uint32(10).string(message.tripId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListDaysRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListDaysRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.tripId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListDaysRequest {
    return {
      tripId: isSet(object.tripId)
        ? globalThis.String(object.tripId)
        : isSet(object.trip_id)
        ? globalThis.String(object.trip_id)
        : "",
    };
  },

  toJSON(message: ListDaysRequest): unknown {
    const obj: any = {};
    if (message.tripId !== "") {
      obj.tripId = message.tripId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListDaysRequest>, I>>(base?: I): ListDaysRequest {
    return ListDaysRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListDaysRequest>, I>>(object: I): ListDaysRequest {
    const message = createBaseListDaysRequest();
    message.tripId = object.tripId ?? "";
    return message;
  },
};

function createBaseListDaysResponse(): ListDaysResponse {
  return { days: [] };
}

export const ListDaysResponse: MessageFns<ListDaysResponse> = {
  encode(message: ListDaysResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.days) {
      Day.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListDaysResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListDaysResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.days.push(Day.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListDaysResponse {
    return { days: globalThis.Array.isArray(object?.days) ? object.days.map((e: any) => Day.fromJSON(e)) : [] };
  },

  toJSON(message: ListDaysResponse): unknown {
    const obj: any = {};
    if (message.days?.length) {
      obj.days = message.days.map((e) => Day.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListDaysResponse>, I>>(base?: I): ListDaysResponse {
    return ListDaysResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListDaysResponse>, I>>(object: I): ListDaysResponse {
    const message = createBaseListDaysResponse();
    message.days = object.days?.map((e) => Day.fromPartial(e)) || [];
    return message;
  },
};

function createBaseUpdateDayRequest(): UpdateDayRequest {
  return { dayId: "", region: "", weather: "", dailySummary: "" };
}

export const UpdateDayRequest: MessageFns<UpdateDayRequest> = {
  encode(message: UpdateDayRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.dayId !== "") {
      writer.uint32(10).string(message.dayId);
    }
    if (message.region !== "") {
      writer.uint32(18).string(message.region);
    }
    if (message.weather !== "") {
      writer.uint32(26).string(message.weather);
    }
    if (message.dailySummary !== "") {
      writer.uint32(34).string(message.dailySummary);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdateDayRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdateDayRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.dayId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.region = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.weather = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.dailySummary = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpdateDayRequest {
    return {
      dayId: isSet(object.dayId)
        ? globalThis.String(object.dayId)
        : isSet(object.day_id)
        ? globalThis.String(object.day_id)
        : "",
      region: isSet(object.region) ? globalThis.String(object.region) : "",
      weather: isSet(object.weather) ? globalThis.String(object.weather) : "",
      dailySummary: isSet(object.dailySummary)
        ? globalThis.String(object.dailySummary)
        : isSet(object.daily_summary)
        ? globalThis.String(object.daily_summary)
        : "",
    };
  },

  toJSON(message: UpdateDayRequest): unknown {
    const obj: any = {};
    if (message.dayId !== "") {
      obj.dayId = message.dayId;
    }
    if (message.region !== "") {
      obj.region = message.region;
    }
    if (message.weather !== "") {
      obj.weather = message.weather;
    }
    if (message.dailySummary !== "") {
      obj.dailySummary = message.dailySummary;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UpdateDayRequest>, I>>(base?: I): UpdateDayRequest {
    return UpdateDayRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UpdateDayRequest>, I>>(object: I): UpdateDayRequest {
    const message = createBaseUpdateDayRequest();
    message.dayId = object.dayId ?? "";
    message.region = object.region ?? "";
    message.weather = object.weather ?? "";
    message.dailySummary = object.dailySummary ?? "";
    return message;
  },
};

function createBaseUpdateDayResponse(): UpdateDayResponse {
  return { day: undefined };
}

export const UpdateDayResponse: MessageFns<UpdateDayResponse> = {
  encode(message: UpdateDayResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.day !== undefined) {
      Day.encode(message.day, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdateDayResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdateDayResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.day = Day.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpdateDayResponse {
    return { day: isSet(object.day) ? Day.fromJSON(object.day) : undefined };
  },

  toJSON(message: UpdateDayResponse): unknown {
    const obj: any = {};
    if (message.day !== undefined) {
      obj.day = Day.toJSON(message.day);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UpdateDayResponse>, I>>(base?: I): UpdateDayResponse {
    return UpdateDayResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UpdateDayResponse>, I>>(object: I): UpdateDayResponse {
    const message = createBaseUpdateDayResponse();
    message.day = (object.day !== undefined && object.day !== null) ? Day.fromPartial(object.day) : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
